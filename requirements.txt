# ============================================================
# Phase 1 dependencies (minimum set), Python 3.10+
# Install: pip install -r requirements.txt
# ============================================================

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Config
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# LLM SDKs
openai>=1.30.0
anthropic>=0.28.0
httpx>=0.27.0

# ============================================================
# Phase 2 / RAG dependencies
# ============================================================
# chromadb>=0.5.0        # Optional alternative vector database
FlagEmbedding            # BGE embedding + BGE reranker
qdrant-client            # Qdrant vector database client
# torch                  # Install separately based on CPU/GPU environment
